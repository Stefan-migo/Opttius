/**
 * Print/receipt utility functions for the cash register page.
 *
 * Currently a placeholder — no print or receipt generation logic
 * was found in page.tsx. This module exists to maintain the extraction
 * pattern and provide a home for future print functionality.
 *
 * ponytail: placeholder — no print/receipt logic found in page.tsx.
 * Add receipt generation here when the feature is implemented.
 */

/** Whether any print functionality is available. */
export function hasPrintSupport(): boolean {
  return false;
}
